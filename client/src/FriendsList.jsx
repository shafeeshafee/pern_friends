import axios from "axios";
import { useState } from "react";

function FriendsList({ list, id }) {
  let propId = id;

  const [editedInfo, setEditedInfo] = useState("");

  return <div></div>;
}

export default FriendsList;
